<?xml version="1.0" encoding="utf-8"?>
<!--
  The mark is drawn for a white background (its plumage blue would disappear on a dark card), so
  it keeps a white tile of its own: invisible against the day card, and what makes it readable on
  the night one. The left corners follow the card's inner radius.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" />
    <corners
        android:bottomLeftRadius="10dp"
        android:topLeftRadius="10dp" />
</shape>
